# スト6 ポータルサイト 要件定義

作成日: 2026-09-19
最終更新: 2026-09-20（段階2 を廃止。同期・存在チェック・新キャラ検出を要件から削除し、index.html から件数表示と最終同期を撤去。17 章）
状態: **ポータルは実装完了（段階2 なし）**

---

## 1. 目的・位置づけ

`スト６/` フォルダの入口となる1枚のページ。ここから各キャラの資料に迷わず辿り着けるようにする。

```
スト６/
├── index.html              ← ポータル（新規）
├── ffmpeg.exe              ← 共用
├── COMBO_DB/             ← 自キャラのコンボ・セットプレイ（既存ツール）
│   ├── テンプレート/
│   ├── ラシード/combo.html
│   └── <キャラ>/combo.html
└── VS_DB/             ← 相手キャラの対策（別ツール、要件_キャラ対策.md）
    ├── テンプレート/
    └── <キャラ>/matchup.html
```

「便利」の定義を次の3つに絞る。

1. **導線**: キャラ → コンボ記録 / キャラ対策 / 解説動画 / 外部資料 へ最短で飛べる
2. **一覧性**: 全キャラの状況（レシピ数、対策数、習得率、お気に入り数、**最終更新日**）が一目で分かる
3. **運用の省力化**: 新キャラフォルダを作ったとき、ポータルへの登録を手作業にしない

---

## 2. 前提・制約

コンボ記録ツールと同じ環境で動かす前提。ここが機能の可否を決める。

| 前提 | 影響 |
|---|---|
| `file://` でダブルクリック起動、サーバー無し | 他ファイルの `fetch()` は不可。**フォルダの自動列挙もできない**。キャラ一覧はポータル自身のデータとして持つ |
| Chrome / Edge（File System Access API） | 「フォルダを接続」すれば `スト６/` 配下の読み書きが可能。キャラフォルダの検出、各 `combo.html` 内 JSON の読み取りができる。接続はブラウザの仕様上、毎回ユーザーのボタン操作が必要 |
| 単一 HTML + 自己書換保存 | combo.html と同じ方式。データは `<script id="portal-data">` に埋め込み、「HTMLを保存」で上書き |
| **localStorage は file:// のページ間で共有される**（Chrome、同一プロファイル。2026-09-19 に実機確認） | ポータルは combo.html が書く `combo_learned:<キャラ>` / `combo_fav:<キャラ>` を直接読める。**習得率とお気に入り数は同期なしで常に最新**。逆に新ツールはキー名の接頭辞を分ける必要がある |
| 外部サービス | YouTube 等はインターネット接続時のみ。サムネイル画像 `img.youtube.com/vi/<id>/hqdefault.jpg` は file:// からでも表示できる。iframe 埋め込みは動画側の設定で拒否されることがあるため、リンクで開く |
| 相対リンク | `COMBO_DB/RASHID/combo.html` のような相対パスは file:// でも動く。**フォルダ名 = キャラ名**で統一する |

---

## 3. 機能要件

優先度: **A** = 最初に作る / **B** = 次に作る / **C** = 保留

### 3.1 キャラ一覧（A）

- キャラカードをグリッド表示。名前、レシピ数、対策数、習得率のバー、お気に入り数、**コンボ最終更新日 / 対策最終更新日**
- カードに「コンボ記録」「キャラ対策」の2ボタン。フォルダが存在しないほうは無効表示
- 並び順: 手動 / 名前 / 習得率 / 最終更新（新しい順）
- 役割タグ（メイン / サブ / 対策のみ）でフィルタ
- キャラ数は最大 26 想定。1画面に収まる

### 3.2 解説動画・外部リンク（A）— **キャラ単位で管理（決定）**

- キャラごとに URL リスト。項目: タイトル、URL、種別（解説 / 試合 / コンボ / 対策）、メモ、**追加日 / 更新日（自動）**
- YouTube URL は動画 ID を抽出してサムネイル表示。クリックで新しいタブ
- キャラに紐づかない汎用リンク（フレームデータサイト、Wiki、大会アーカイブ等）も同じ仕組み
- 編集モードで追加・削除・並び替え。タイトルは手入力（file:// では URL からの自動取得ができない）
- レシピ単位の参考動画は持たない

### 3.3 ~~スナップショット同期~~ — **廃止（2026-09-20、17章）**

- 「フォルダを接続」→ `COMBO_DB/*/combo.html` と `VS_DB/*/matchup.html` を読み、**件数と最終更新日だけ**をポータルの HTML に保存する
  - 最終更新日 = データ内の `updated` の最大値。無い場合（ラシード原本など旧データ）はファイルの `lastModified` を使う
  - `<meta name="sf6-tool">` でツール種別、`sf6-tool-version` でバージョンを読み取り、古いバージョンには印を付ける
- 習得率・お気に入り数は localStorage から直接（常に最新）
- レシピ本文は保存しない。**横断検索は保留（C）**（キャラ対策の決定 #6: ポータルからキャラを選んで各ツールに届けばよい）
- デメリットと対策は 5 章（本文を持たないので、旧 4 章の #4・#7 は解消）

### 3.4 ~~新キャラの検出・登録~~ — **廃止（2026-09-20、17章）**。キャラ登録は手動のみ

- 接続時に `COMBO_DB/` と `VS_DB/` 配下のフォルダを列挙し、未登録のフォルダがあれば「追加しますか」と提案
- 登録済みでフォルダが見つからないキャラは警告表示
- **フォルダ作成そのものは手順書のまま**（テンプレートのコピーはエクスプローラーで行う）
- テンプレート更新の既存キャラへの反映機能は **保留（C）**

### 3.5 キャラ対策への導線（A）

- キャラカードから `VS_DB/<キャラ>/matchup.html` を開く
- キャラ対策ツールの中身は [要件_キャラ対策.md](要件_キャラ対策.md) で定義済み。ポータル側はリンクのみ

### 3.6 共通機能（A）

- 編集モード切替、HTMLを保存、JSON書き出し／読み込み（combo.html と同じ操作感）
- ダークテーマ、combo.html と同じ配色・フォント
- キーボード: `/` で検索欄フォーカス

### 却下・保留

- 練習ログ: **不要（決定）**
- テンプレート更新の反映: 保留
- レシピ単位の参考動画: 不要
- 全文の横断検索: 廃止
- フォルダ接続による件数・更新日の同期、存在チェック、新キャラ検出: **廃止（2026-09-20）**

---

## 4. ポータルとして満たす横断要件

個別機能とは別に、ポータル全体で守ること。

### 4.1 日付

| 対象 | 持つ日付 | 付け方 | 表示 |
|---|---|---|---|
| キャラ（登録） | 登録日 | ポータルで追加した日 | カード展開時 |
| ~~キャラ（コンボ記録）~~ | ~~最終更新日~~ | 同期廃止のため持たない | — |
| ~~キャラ（キャラ対策）~~ | ~~最終更新日~~ | 同期廃止のため持たない | — |
| 参考情報（キャラ別・全体） | 追加日 / 更新日 | ポータルのフォーム保存時に自動 | 参考情報一覧 |
| ~~スナップショット~~ | ~~最終同期日~~ | 同期廃止 | — |
| ポータル自身 | 最終保存日 | HTML保存時に自動 | フッター |

- 日付はすべて `YYYY-MM-DD` の文字列。ツール側（combo.html / matchup.html）も同じ形式で `created` / `updated` を持つ（**コンボ記録テンプレートは 2026-09-19 に対応済み**）
- 「最近の更新」欄: 全キャラを最終更新日（参考情報の更新日と登録日から算出）の新しい順に並べ、上位数件を画面上部に出す

### 4.2 ~~整合性~~ — 廃止（2026-09-20）

フォルダ接続をしないため、存在チェック・バージョン検出は行わない。リンク先が無い場合はブラウザのエラー表示に任せる。

### 4.3 バックアップと復元

- JSON書き出し／読み込み（他ツールと同じ）
- ~~上書き前の `index.html` を `backup/` に残す~~ → フォルダ接続をしないため不可。JSON書き出しで代用

### 4.4 使い方の案内

- 画面下部に折りたたみの「使い方」: 新キャラの追加手順（テンプレートのコピー → キャラ名設定 → ポータルで接続して登録）、ffmpeg の置き場所、対応ブラウザ
- 初回起動（キャラ0件）のときは案内を開いた状態にする

### 4.5 表示

- キャラ名の表示名とフォルダ名を分けて持つ（記号を含む名前への備え）
- ダークテーマ、他ツールと同じ配色。1画面に 26 キャラが収まるカードサイズ
- ネット未接続でもサムネイル以外は崩れない

---

## 5. スナップショット方式のデメリットと対策

| # | デメリット | 具体的に何が起きるか | 対策 |
|---|---|---|---|
| 1 | **件数・更新日の鮮度が落ちる** | combo.html でレシピを追加・削除しても、ポータルの件数と最終更新日は「接続 → 同期 → HTMLを保存」するまで古いまま | カードに「最終同期 9/19」を出して古さを見えるようにする。接続したら自動で同期する。習得率・お気に入りは localStorage から読むので鮮度の問題がない |
| 2 | **同期に手作業が要る** | file:// では接続なしに他ファイルを読めないので、同期のたびにフォルダ選択ダイアログを操作する。自動化はできない | 接続ボタンを1つにまとめ、接続 = 同期にする。Chrome は前回のフォルダを覚えるので2回目以降はダイアログで「選択」を押すだけ |
| 3 | **同じ情報が2箇所にある** | 件数や更新日をポータル側で直したくなる。直すと次の同期で消える | 集計値はポータルで編集不可にする。カードから各ツールへ飛んで編集する |
| 4 | ~~index.html が大きくなる~~ | 集計のみ保存する方針に変えたため解消 | — |
| 5 | **改名・削除に追従しない** | キャラフォルダを改名したり combo.html を消したりしても、ポータルには古いスナップショットとリンクが残り、クリックするとファイルが無い | 接続時に存在チェックし、見つからないキャラに警告を出す（3.4）。削除は手動確認つき |
| 6 | **別ブラウザ・別PCでは習得率が出ない** | localStorage はそのブラウザにしかないので、別環境で開くとスナップショットに含まれる `learned` / `fav` フラグ（combo.html を保存した時点の値）に落ちる | combo.html 側で「HTMLを保存」しておけばフラグは HTML に残る。運用で吸収 |
| 7 | ~~ポータルを人に渡すと全キャラ分のレシピとメモが付いてくる~~ | 集計のみ保存する方針に変えたため解消。リンクと件数だけになる | — |

総じて、**鮮度（#1）と手作業（#2）が実運用で気になる点**。それ以外は規模が小さいうちは問題にならない。

---

## 6. 画面構成案

```
┌──────────────────────────────────────────────┐
│ スト6                    [検索 ______] [接続] [編集]│
├──────────────────────────────────────────────┤
│ 最近の更新: ラシード(9/19) ケン(9/15) …   同期 9/19│  ← 4.1
│ 共通リンク: フレームデータ / Wiki / 大会 ...       │  ← 3.2 汎用リンク
├──────────────────────────────────────────────┤
│ [並び順: 手動 名前 習得率 同期] [役割: メイン サブ 対策]│
│                                              │
│ ┌────────────┐ ┌────────────┐                │
│ │ラシード     │ │ ケン        │                │  ← 3.1 キャラカード
│ │ｺﾝﾎﾞ10 対策4│ │ ｺﾝﾎﾞ0 対策0 │                │
│ │■■□ ★3      │ │             │                │
│ │更新 9/19    │ │ 未同期      │                │
│ │[コンボ][対策]│ │[コンボ][対策]│                │
│ └────────────┘ └────────────┘                │
├──────────────────────────────────────────────┤
│ ▼ ラシード（カード展開時）                        │
│   解説動画  ▣ ▣ ▣  (サムネイル、追加日)           │  ← 3.2
│   登録 9/01 / コンボ更新 9/19 / 対策更新 9/15      │  ← 4.1
├──────────────────────────────────────────────┤
│ ▶ 使い方                              最終保存 9/19│  ← 4.4
└──────────────────────────────────────────────┘
```

---

## 7. データモデル

```json
{
  "version": 1,
  "saved": "2026-09-19",
  "synced": "2026-09-19",
  "links": [
    { "title": "フレームデータ", "url": "https://...", "kind": "資料", "note": "",
      "created": "2026-09-01", "updated": "2026-09-01" }
  ],
  "chars": [
    {
      "name": "ラシード",
      "folder": "ラシード",
      "role": "メイン",
      "created": "2026-09-01",
      "links": [
        { "title": "基礎解説", "url": "https://youtu.be/xxxx", "kind": "解説", "note": "",
          "created": "2026-09-10", "updated": "2026-09-10" }
      ],
      "combo":   { "exists": true, "total": 10, "updated": "2026-09-19", "version": 2 },
      "matchup": { "exists": false, "total": 0, "updated": null, "version": null }
    }
  ]
}
```

- `folder` はフォルダ名。`COMBO_DB/<folder>/combo.html` と `VS_DB/<folder>/matchup.html` の両方に使う
- `combo` / `matchup` は同期時に上書きされる集計値。レシピ本文は持たない
- 習得数・お気に入り数は保存せず、表示時に localStorage（`combo_learned:<name>` 等）から数える

---

## 8. 決定事項（2026-09-19）

| 論点 | 決定 |
|---|---|
| 動画リンクの粒度 | キャラ単位。レシピ単位は持たない |
| スナップショット方式 | 採用。ただしキャラ対策の決定（横断検索不要）を受けて**件数と更新日のみ**に縮小。デメリットは 5 章 |
| 新キャラ追加・テンプレート反映 | 最低限（フォルダ検出と登録提案のみ）。フォルダ作成は手順書、テンプレート反映は保留 |
| 対策表・練習ログ | 対策は **別ツール** として `VS_DB/<キャラ>/matchup.html` に置く（要件_キャラ対策.md、確定済み）。練習ログは不要 |
| 更新日 | 各情報に作成日・更新日を持つ（4.1）。コンボ記録テンプレートは対応済み |
| ffmpeg.exe | `スト６/` に移動済み。compress.bat は2階層上まで探す |
| フォルダ構成 | `COMBO_DB/<キャラ>/` と `VS_DB/<キャラ>/` に分離済み |

---

## 9. 実装の進め方

| 段階 | 内容 | combo.html 側の改修 |
|---|---|---|
| 1 | **実装済み（2026-09-19）** キャラ一覧 + リンク管理 + 日付 + 使い方 + 保存（3.1 / 3.2 / 3.5 / 3.6 / 4.1 / 4.4）。習得率は localStorage から | テンプレートのヘッダに「← 一覧」リンクを追加済み |
| ~~2~~ | ~~接続 → 件数・更新日の同期 + 存在チェック + 新キャラ検出 + バックアップ~~ | **廃止（2026-09-20）** |
| 3 | キャラ対策ツール（テンプレート）を作り、導線を有効化 | — |

### 段階1 の実装メモ

- ファイル: `スト６/index.html`（約 690 行、依存なし）。`<meta name="sf6-tool" content="portal">` / `sf6-tool-version=1`
- 初期データとして ラシード（メイン、フォルダ `ラシード`）を登録済み
- 段階2 前の制限: 件数（コンボ / 対策）は「—」、カードに「未同期」表示、フォルダの存在チェックなし（両方のボタンが常に有効）
- 習得数・お気に入り数は `combo_learned:<name>` / `combo_fav:<name>` から。**総数が無いので習得率バーは段階2で同期するまで出ない**。並び順「習得率」は当面「習得数」で代用
- 「最近の更新」は、同期前はリンクの更新日とキャラ登録日から算出
- 検索はカタカナ→ひらがな、全角英数→半角に正規化して照合（「けん」で「ケン」が当たる）
- 他タブの combo.html で習得状況を変えると `storage` イベントで自動再描画
- URL は `http(s)://` 以外を拒否。YouTube のサムネイルは `youtu.be` / `watch?v=` / `shorts` / `embed` / `live` の形式から ID を抽出
- 自動テスト（headless Chrome）: 表示、キャラ追加、重複フォルダ拒否、リンク追加とサムネイル、不正URL拒否、共通リンク、最近の更新、並び順、検索、HTML生成のクリーンアップ、削除を確認済み

段階1だけでも「導線」の目的は達成できる。

---

## 10. 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-09-19 | 提案版作成 → 決定事項を反映して確定 → 横断要件（日付・整合性・バックアップ・案内）を追加 |
| 2026-09-19 | 段階1 実装。`index.html` を作成し、テンプレート combo.html に「← 一覧」リンクを追加 |
| 2026-09-20 | 11章コメントを受けて、要件変更（並び順・役割フィルタ削除、メニュー3項目化、参考情報モデル）と新デザイン3案を 12 章に追記。モック `デザイン案_ポータル.html` を作成 |
| 2026-09-20 | C ライト・ポップが採用方向。C派生5案（C1〜C5）とキャラ画像の仕様を 13 章に追記。モック `デザイン案_ポータル_C派生.html` を作成（実画像 `画像/` を使用） |
| 2026-09-20 | C2 ポスターに決定（画像の濃さ・ホバー演出はモック値で確定）。「カードの白い余白が目立つ」への改善4案（P1〜P4）を 14 章に追記。モック `デザイン案_ポータル_C2改.html` |
| 2026-09-20 | P1 コンパクトに決定。新要件「自キャラタグで上段に分ける」「横6列」を追加し、レイアウト4案（Q1〜Q4）を 15 章に追記。モック `デザイン案_ポータル_6列.html` |
| 2026-09-20 | Q3 に決定（その他の枠は短縮ラベル、自キャラバッジなし）。新デザイン版 `index.html`（v2）を実装。旧 v1 データは起動時に自動移行 |
| 2026-09-20 | 段階2（同期・存在チェック・新キャラ検出・バックアップ）を廃止。index.html からコンボ/対策の件数表示と「最終同期」を撤去 |
| 2026-09-20 | キャラ対策テンプレートを実装。ポータルの導線先が揃った |
| 2026-09-20 | コンボ記録・キャラ対策の両テンプレートをポータルと同じライト・ポップに統一（種別バナー: コンボDB 青 / VSキャラ対策DB ピンク）。3ツールの見た目が揃った |
| 2026-09-20 | 3画面共通の背景画像（グレースケール）の敷き方5案を 18 章に追記。モック `デザイン案_背景.html` |
| 2026-09-20 | B1・色あり 45%・グレースケール切替可で決定。スクロール時に次の画像へ移る方式5案を 18.5 に追記。モック `デザイン案_背景2.html` |
| 2026-09-20 | 背景画像（B1 固定・1枚）を3ツールに実装。ポータルはラシードの `y_64a3cd87d8348.jpg`、ラシードのコンボDB は `topic-08152-1.jpg` を既定に設定 |
| 2026-09-20 | コンボDB / VS DB の種別バナーを廃止し、タイトルを「キャラ名 COMBO」（ピンク）「相手キャラ名 VS」（青）に。フィルタの背景板と固定を廃止 |
| 2026-09-20 | 3画面共通のカードデザイン5案（K1〜K5）を 19 章に追記。モック `デザイン案_カード.html` |
| 2026-09-20 | コンボDB / VS DB は K5（ガラス）に決定。ポータルのカードは SF6 ロゴ風の幾何学デザイン4案（G1〜G4）を 20 章に追記。モック `デザイン案_ポータルカード.html` |
| 2026-09-20 | G1 採用。ポータルのカードを G1 で実装し、六角紋章にキャラごとの紋章画像（既定はタイトルロゴ）を表示する仕組みを追加 |
| 2026-09-20 | コンボDB / VS DB に K5（ガラス）を適用 |
| 2026-09-20 | ポータル: 紋章の縁をキャラ色から黒に統一、既定の紋章画像を `画像/StreetFighter6/LTyBkxp-_400x400.jpg` に変更、ボタン表記を「COMBO DB / VS DB / Information」（Bebas Neue、中央揃え）に |
| 2026-09-20 | カードを「全面画像 + 右下に白い名前、紋章なし」にする要望。5案（F1〜F5）を 21 章に追記。モック `デザイン案_ポータルカード2.html` |
| 2026-09-20 | F5 採用。ポータルのカードを F5 で実装、紋章を撤去。「その他のキャラをグレースケールにし、カーソルを当てたときだけカラー」モードを追加（既定 ON） |
| 2026-09-20 | デザイン案のモック（`デザイン案_*.html`）を `作業証跡/デザイン案/` に退避。一覧と決定は `作業証跡/README.md` |
| 2026-09-20 | 英語のキャラ名で Teko 700 が太すぎるとの指摘。名前フォント8案（T0〜T7）を 22 章に追記。モック `デザイン案_名前フォント.html` |
| 2026-09-20 | T2 採用。キャラ名を Bebas Neue に変更（Teko を廃止）。日本語名は Noto Sans JP 700 のまま、`font-synthesis: none` で Bebas の擬似ボールドを防止 |
| 2026-09-20 | フォルダ名変更に追従: ボタンのリンク先を `COMBO_DB/<フォルダ名>/combo.html`、`VS_DB/<フォルダ名>/matchup.html` に |
| 2026-09-20 | キャラ色（`color`、カラーピッカー）を削除。画像のないカードは炭色 + 斜線。データの `color` は読み込み時に捨てる |
| 2026-09-20 | 背景画像の選択を「登録キャラのフォルダのみ」から「画像/ 配下ならどこでも」に変更（画像フォルダを接続して相対パスを自動取得。パスの直接入力も可） |
| 2026-09-20 | `画像/` 配下のフォルダ名をアルファベットに変更（ラシード → RASHID、ケン → KEN、タイトルロゴ → StreetFighter6）。保存済みパス（ポータル、RASHID のコンボDB、モック、文書）を一括更新。ポータル背景の誤った保存パス `画像/RASHID/mv_edition_bg.jpg` を実体のある `画像/StreetFighter6/` に修正 |
| 2026-09-20 | 3画面の「使い方」に背景画像の選び方（大きさ・明るさ・構図・避けるもの・濃さ・形式、画面ごとの向き）を追記。ポータルはカード画像の選び方も |
| 2026-09-20 | その他のキャラ（小タイル）のボタンを省略形「COMBO / VS / INFO」に。文字サイズを 15px に |
| 2026-09-20 | その他のキャラを横8列に。本文の最大幅を 1240 → 1650px に広げ、小タイルの大きさ（約187px）は維持。狭い画面では列数が自動で減る（1280px 幅では 6 列） |
| 2026-09-20 | カードの習得数・お気に入り数の表示を削除（localStorage の読み取りと `storage` イベントも撤去）。カードの統計は更新日のみ |
| 2026-09-20 | Information / INFO ボタンの件数表示を削除 |

## 11.コメント
ポータルについて
ポータルサイトのUI、デザインはコンボツールと合わせる必要はない。色んなキャラが登場するゲームなので鮮やかな新デザインの適用を期待する。
また、並び順や役割といった並び替えやフィルタの機能はいらないので削除。
キャラのメニューは「コンボ記録」「キャラ対策」「参考情報」（情報の概要の自由記述＋URL）といった形で使いやすいものにしたい。

---

## 12. 新デザイン提案（11章コメントへの回答）

### 12.1 コメントを要件に反映したもの

| コメント | 反映 |
|---|---|
| UI・デザインはコンボツールと合わせなくてよい。鮮やかな新デザインを | 3章の「combo.html と同じ配色・フォント」を撤回。12.3 の3案から選ぶ。キャラごとに固有色を持たせ、色で識別できるようにする |
| 並び順・役割のフィルタは不要 | 3.1 の並び順・役割タグを削除。並びは記述順（編集モードの↑↓）のみ。検索欄は残す |
| キャラのメニューは「コンボ記録」「キャラ対策」「参考情報」 | カードのメニューを3項目に固定。「参考情報」= 概要の自由記述 + URL の一覧（旧「解説動画・外部リンク」を置き換え。種別・タイトルは廃止し、**概要文と URL だけ**にする） |

**参考情報のデータ**（キャラごとの配列）

```json
{ "text": "基礎コンボと起き攻めの解説。12:30 から中スパイク〆の話", "url": "https://youtu.be/...", "created": "2026-09-19", "updated": "2026-09-19" }
```

- `text` が見出し兼メモ。URL は任意（メモだけの参考情報も可）
- YouTube のサムネイルは出さない方針に変更（概要文が主役。必要なら後で追加）
- 共通リンク（キャラに紐づかない資料）は「全体の参考情報」として画面下部に同じ形式で残す

**変わらないもの**: 単一 HTML・自己書換保存、日付（4.1）、習得数・お気に入り数の localStorage 読み取り、最近の更新、使い方、段階2 の同期方針

### 12.2 デザインの共通方針

- **キャラ固有色**: 各キャラに `color`（16進）を持たせ、編集フォームで選べる。既定は 26 色のパレットから順に割り当て
- **キャラ画像は使わない**（著作権）。代わりに固有色 + 大きな名前 + 頭文字の透かしで「キャラらしさ」を出す
- 見出しは欧文コンデンス体（Bebas Neue、Google Fonts）。オフラインでは Noto Sans JP / システムフォントに落ちる。**フォント読み込みに失敗しても崩れない**ことを条件にする
- カードの3ボタンは常時表示。「参考情報」を押すとカード内に展開（案 B はパネル表示）
- スマホ幅でも1列で成立する

### 12.3 3案（モック: `デザイン案_ポータル.html`、上部タブで切替）

| 案 | 雰囲気 | 構成 | 向いている人 |
|---|---|---|---|
| **A ストリート** | 黒地に斜めの虹色帯、グラフィティ風。ゲームの UI に近い | カードグリッド。カード上辺がキャラ色、名前が大きく、下に3ボタン（COMBO / VS / INFO）。参考情報はカード内に展開 | 一覧で全キャラを見渡しつつ、派手さが欲しい |
| **B キャラセレクト画面** | ゲームのキャラ選択画面そのもの。正方形タイルにキャラ色のグラデーション | 左にタイル一覧、右に固定パネル。タイルをクリックするとパネルに 01 コンボ記録 / 02 キャラ対策 / 03 参考情報 と参考情報一覧が出る | キャラ数が増えても一覧が崩れない。「選んでから見る」操作が好み |
| **C ライト・ポップ** | 白地に太い黒線、キャラ色の円。明るく軽い | カードグリッド。角丸カードに縦並びの3ボタン、右上にキャラ色の大きな円。参考情報はカード内に展開 | 昼間に見やすい明るい画面が良い。黒背景に飽きた |

**推奨は B**。理由は、キャラが増える前提（最大 26）でもタイル一覧が崩れず、メニュー3項目を大きく押しやすい形で置けるため。A は一覧性と派手さのバランスが良く、C は明るさが唯一の差別化点。

### 12.4 決めてほしいこと

1. **案の選択**: A / B / C、または「A の配色で B の構成」のような組み合わせ
2. **参考情報のサムネイル**: 出さない（提案）/ YouTube だけ出す
3. **キャラ固有色の決め方**: 自動割り当てのみ / 編集フォームで変更可（提案）

決定後、`index.html` を新デザインで作り直す（段階1 の機能はそのまま移植）。段階2 はその後。

---

## 13. C案の派生とキャラ画像（2026-09-20）

12.3 の **C ライト・ポップ** を基本デザインに決定。白地 + 太い黒線 + 硬い影 + キャラ色の円 + 縦並び3ボタン、という骨格は共通で、5つの派生を用意した。

### 13.1 派生5案（モック: `デザイン案_ポータル_C派生.html`、上部タブで切替）

| 案 | 変えたところ | 画像の見せ方 | 印象 |
|---|---|---|---|
| **C1 クラシック + 画像** | 前回の C そのまま | 右下に乗算 20%、ホバーで 60% | 一番おとなしい。画像は「気配」程度 |
| **C2 ポスター** | カードを縦長に。3ボタンをカード下部のキャラ色の帯に横並び | 40%、ホバーで 90%。画像が主役 | キャラの存在感が強い。1行あたりのカード数は減る |
| **C3 ロスター（横長）** | 横長カード2列。円形アイコン + 名前 + 横並び3ボタン | キャラ色の円の中に収める（乗算で白背景がキャラ色に） | 一覧性が高い。26キャラでも縦に長くなりすぎない |
| **C4 ソフト** | 黒線と硬い影をやめ、キャラ色を10%混ぜた淡いカードと柔らかい影 | 16%、ホバーで 50% | 落ち着いた印象。長時間見ても疲れにくい |
| **C5 ビビッド** | カード全面をキャラ色に。白文字 + 黒影 | 45%、ホバーで 85%。色に溶け込む | 最も鮮やか。画像なしのキャラも色だけで映える |

**推奨: C2 か C3。** 画像を活かしたいなら C2、キャラが増えることを重視するなら C3。C1/C4 は画像が控えめすぎて「浮かび上がる」実感が弱い。C5 は好みが分かれる。組み合わせ（例: C3 の一覧 + クリックで C2 のカードに展開）も可能。

### 13.2 キャラ画像のカスタム仕様

**配置**: `スト６/画像/<キャラ名>/` に任意のファイル名で置く（現状: `画像/RASHID/images.jpg`、`画像/KEN/images (1).jpg`）

**表示の仕組み**

- `<img>` を `mix-blend-mode: multiply` で重ねる。**白背景の画像は白が消えて絵だけが残る**ので、切り抜き不要。透過 PNG でも同じ結果
- 既定は低い不透明度（案ごとに 16〜45%）で「うっすら」、ホバーで濃くなり少し動く（浮かび上がる演出）
- 画像が無いキャラは頭文字の透かし（C3 は円の中に頭文字）
- 画像サイズは小さくてよい。今の 210×240px でもカード内では十分。推奨は 300〜600px、白背景か透過 PNG。大きすぎる画像は読み込みが遅くなるだけ

**登録の方法**（file:// ではフォルダの中身を列挙できないため、ファイル名をデータに持つ）

| 方法 | 操作 | 段階 |
|---|---|---|
| 編集フォームで選ぶ | 「画像を選ぶ」→ `画像/<キャラ名>/` の中のファイルを選択。ファイル名だけ受け取り `画像/<キャラ名>/<ファイル名>` を保存 | 段階1（新デザイン版） |
| カードにドロップ | 編集モードでカードに画像ファイルをドロップ。同上 | 段階1（新デザイン版） |
| 自動検出 | フォルダ接続時に `画像/<キャラ名>/` の最初の画像を拾う | 段階2 |

- 選んだ直後はブラウザ内の blob でプレビューし、保存後は相対パスで読む。**フォルダ以外の場所から選ぶと保存後に表示されない**ので、フォームに「画像/<キャラ名>/ に置いたファイルを選んでください」と表示する
- 表示されないときはカードに「画像が見つかりません: <パス>」を編集モードでだけ出す

**データ**（キャラごと）

```json
{ "name": "ラシード", "folder": "ラシード", "color": "#00c9a7", "image": "画像/RASHID/images.jpg", ... }
```

- `image` は `スト６/` からの相対パス。空なら頭文字の透かし
- `color` はキャラ固有色。編集フォームのカラーピッカーで変更可（12.4 の提案どおり）

**著作権**: 画像は個人利用の範囲。配布用書き出しは作らない方針（5章 #7）のままなので問題は小さいが、`index.html` を人に渡す場合は画像フォルダごと渡さない限り表示されない、と認識しておく

### 13.3 決めてほしいこと

1. C1〜C5 のどれにするか（組み合わせ可）
2. 画像の既定の濃さ: モックの値でよいか、もっと薄く/濃く
3. ホバーで浮かび上がる演出の要否（不要なら常時一定）

決定後、`index.html` を新デザインで作り直す（段階1の機能 + 参考情報モデル + 画像・色の編集）。

---

## 14. C2 の白余白の改善（2026-09-20）

**決定**: 13.1 の **C2 ポスター** を採用。画像の既定の濃さ（40%、ホバーで 90%）とホバー演出はモックの値で確定。
**指摘**: カードの白い面が広く目立つ（特に画像のないキャラ）。

### 14.1 改善4案（モック: `デザイン案_ポータル_C2改.html`、上部タブで切替）

| 案 | 何を変えたか | 白の残り方 | 画像なしキャラ | 備考 |
|---|---|---|---|---|
| **P1 コンパクト** | カード高さを 380 → 約 260px に詰め、画像を右半分いっぱいに | 文字の周りだけ | 頭文字の透かし + キャラ色のドット柄 | C2 に最も近い。1画面のキャラ数が増える |
| **P2 カラー下地** | 上部を白→キャラ色の淡いグラデにし、右下にキャラ色の円 | ほぼ無し（左上のみ淡く白） | 円と頭文字で埋まる | 柔らかい。画像はグラデの上で乗算 50% |
| **P3 対角カット** | 右側を斜めにキャラ色で塗り、画像をその上に。ボタン帯は黒 | 文字側の左半分だけ | 斜め面に白い頭文字 | 最も動きがある。画像が色面の上に乗るので濃く出る |
| **P4 横型（画像パネル）** | 横長カード。左に名前と縦3ボタン、右はキャラ色の全面パネルに画像 | ゼロ（左の文字面は必要な白） | パネルに白い頭文字 | 2列で一覧性が高い。3ボタンは縦積み（C の形） |

**推奨: P3 か P4。** 白を減らす効果が最も大きく、画像なしキャラでも寂しくならない。P3 は C2 の縦カード + 下帯の形を保ち、P4 は一覧性を優先する。P1 は「C2 のまま少し詰めた」程度、P2 は淡い分だけ鮮やかさが落ちる。

### 14.2 実装上の注意（モックで判明）

- 画像の `mix-blend-mode: multiply` は、**画像の親に `z-index` を付けるとブレンドが効かなくなる**（スタッキングコンテキストで隔離される）。装飾の色面は `::before`、画像はその後の子要素として置き、`z-index` を使わずに重ね順を DOM 順で決める
- 画像はカード上部（ヘッド）の中に閉じ込め、ボタン帯に重ねない
- 画像のないキャラは、頭文字の透かしに加えて色面やドット柄で白を埋める

### 14.3 決めてほしいこと

1. P1〜P4 のどれにするか（組み合わせ可。例: P3 の色面 + P1 の高さ）
2. P3 を選ぶ場合、ボタン帯を黒にするか、C2 のようにキャラ色のままにするか

---

## 15. 自キャラタグと横6列（2026-09-20）

**決定**: 14.1 の **P1 コンパクト** を採用。
**新要件**:

1. キャラに「自キャラ」タグを付けられる。タグ付きのキャラは画面上部の「自キャラ」枠にまとめて表示し、それ以外は下の「その他のキャラ」枠に並ぶ
2. カードは横6列で並べる（1240px 幅で1枚 約190px）

### 15.1 要件への反映

- データ: キャラに `mine: true` を持つ（編集フォームのチェックボックス「自キャラ」）。12章で廃止した「役割」の代わりになる
- 表示: 「MY CHARACTERS 自キャラ」「ROSTER その他のキャラ」の2セクション。各セクション内は記述順（編集モードの↑↓で並べ替え。セクションをまたぐ移動はタグの付け外しで行う）
- 6列: `grid-template-columns: repeat(6, 1fr)`。1100px 未満では 170px 以上を確保して列数を自動で減らす（スマホは2列）
- 190px 幅に収めるための調整
  - 名前は 30px。**5文字以上は 25px、6文字以上は 21px、7文字以上は 18px** に自動で縮める（ディージェイ、ザンギエフ対策）
  - 統計は「ｺﾝﾎﾞ 10 / 習得 2 / ★ 1」の1行、11px
  - 画像は右寄せ 42%、ホバー 90%（P1 と同じ）
  - 自キャラカードの右上にピンクの「自キャラ」バッジ

### 15.2 レイアウト4案（モック: `デザイン案_ポータル_6列.html`、上部タブで切替）

| 案 | 自キャラ枠 | その他の枠 | 3ボタン | 特徴 |
|---|---|---|---|---|
| **Q1 統一6列・縦積み** | 6列タイル | 6列タイル | 帯の中に縦3段（フルラベル） | P1 をそのまま縮小。ボタンが読みやすい分、帯が高い（約 115px） |
| **Q2 統一6列・3連** | 6列タイル | 6列タイル | 帯の中に横3連（コンボ / 対策 / 参考 + 件数） | 帯が薄く（約 55px）画像が大きく見える。ラベルは短縮 |
| **Q3 自キャラは大きく** | **P1 サイズ（3列）** | 6列タイル（3連ボタン） | 自キャラはフルラベル横並び、他は3連 | 使うキャラが目立ち、対策用キャラは一覧として並ぶ。上下でカードの格が変わる |
| **Q4 縦書き名前** | 6列タイル | 6列タイル | 縦3段 | 名前を縦書きにして右端に立て、画像を左に。狭い幅でも名前が大きい。カードは縦長（約 370px） |

**推奨: Q3。** 「自キャラ」枠を設ける意図（メインで使うキャラを目立たせる）に最も合い、その他のキャラは6列で一覧できる。全部を同じ大きさにしたいなら Q2（帯が薄く画像が活きる）。Q1 は堅実、Q4 は個性が強い。

### 15.3 決めてほしいこと

1. Q1〜Q4 のどれにするか
2. その他の枠の3ボタンを短縮ラベル（コンボ / 対策 / 参考）にしてよいか
3. 「自キャラ」バッジの要否（枠で分かれているので省いてもよい）

決定後、この見た目で `index.html` を作り直す。

---

## 16. 新デザイン版 index.html の実装（2026-09-20）

**決定**: 15.2 の **Q3**（自キャラは P1 サイズ 3列、その他は6列タイル）。その他の枠の3ボタンは短縮ラベル（コンボ / 対策 / 参考）。「自キャラ」バッジは無し。

### 16.1 実装内容（`index.html`、`sf6-tool-version` = 2）

| 区分 | 内容 |
|---|---|
| 画面 | ヘッダ（SF6 PORTAL、検索、編集）→ 最近の更新 → **MY CHARACTERS 自キャラ**（3列・大）→ **ROSTER その他のキャラ**（6列・小）→ 全体の参考情報 → 使い方 → フッター（最終保存 / 最終同期） |
| カード | キャラ色の帯に3ボタン。大カードは「コンボ記録 / キャラ対策 / 参考情報」、小タイルは「コンボ / 対策 / 参考」。参考情報ボタンのみ件数付き。習得数・★は localStorage から。更新日（参考情報の更新日と登録日の新しいほう） |
| 画像 | `image` の相対パスを乗算 42% で表示、ホバー 90%。無ければ頭文字の透かし + キャラ色のドット柄。読めない場合は編集モードでだけ「画像が見つかりません: パス」を表示 |
| 名前 | 6列タイルでは 5文字以上 25px、6文字以上 21px、7文字以上 18px に自動縮小 |
| 参考情報 | 「参考」ボタンでカード内に展開（カードが行いっぱいに広がる）。1件 = 概要 + URL（任意）+ 作成日/更新日。編集モードで追加・編集・削除・↑↓ |
| 全体の参考情報 | 画面下部にピル表示。キャラに紐づかない資料用 |
| 編集モード | キャラの追加・編集・削除・↑↓（**同じ枠の中でのみ移動**。枠をまたぐには「自キャラ」チェックを変える）。フォーム: キャラ名、フォルダ名、キャラ色（カラーピッカー）、自キャラ、画像 |
| 画像の登録 | フォームの「画像を選ぶ」か、編集モードでカードに画像ファイルをドロップ。`画像/<フォルダ名>/<ファイル名>` を記録し、選んだ直後はブラウザ内でプレビュー |
| 保存 | HTML自己書換（`showSaveFilePicker`、非対応時はダウンロード）。JSON書き出し／読み込み |
| データ移行 | v1（`role` / `links`）を起動時と JSON 読み込み時に v2（`mine` / `refs` / `color` / `image`）へ自動変換 |
| 検索 | キャラ名・フォルダ名・参考情報の概要。かな種別と全角半角を吸収。`/` でフォーカス、Esc でクリア |
| 廃止 | 並び順・役割フィルタ、リンクの種別・タイトル、YouTube サムネイル |

### 16.2 データモデル（v2）

```json
{
  "version": 2, "saved": "2026-09-20",
  "links": [ { "text": "フレームデータ（公式）", "url": "https://...", "created": "…", "updated": "…" } ],
  "chars": [
    { "name": "ラシード", "folder": "ラシード", "mine": true, "color": "#00c9a7",
      "image": "画像/RASHID/images.jpg", "created": "2026-09-19",
      "refs": [ { "text": "…", "url": "https://…", "created": "…", "updated": "…" } ] }
  ]
}
```

`synced` / `combo` / `matchup` は持たない（読み込んだデータにあれば移行時に捨てる）。

### 16.3 確認したこと（headless Chrome）

- 表示: 自キャラ2件（画像あり）+ その他7件（長い名前、画像なし、存在しない画像パス）でレイアウト崩れなし、JS エラーなし
- 機能: v1 データの移行、キャラ追加（その他 → 自キャラへ切替）、短縮ラベル、名前の縮小クラス、参考情報の展開・追加・不正 URL 拒否、枠内の並べ替え、かな検索、最近の更新、全体の参考情報、保存用 HTML の生成（動的部分のクリア）、削除

### 16.4 残作業

- ~~キャラ対策ツール（`VS_DB/テンプレート/matchup.html`）~~ → 2026-09-20 実装済み（要件_キャラ対策.md 8章）。ポータルの「キャラ対策」ボタンは `VS_DB/<フォルダ名>/matchup.html` を開く
- モック3ファイル（`デザイン案_ポータル*.html`）は決定後の参考用。不要になったら削除してよい

---

## 17. 段階2 の廃止（2026-09-20）

**指示**: フォルダ接続による件数と更新日の同期はポータルに表示しなくてよい。存在チェック、新キャラ検出も不要。

**要件への反映**

- 3.3（同期）、3.4（新キャラ検出）、4.2（整合性）を廃止。4.3 のバックアップもフォルダ接続が前提だったため廃止し、JSON書き出しで代用
- ポータルは **フォルダ接続を一切行わない**。読むのは自身の HTML 内データと localStorage（習得・★）だけ
- カードから消したもの: コンボ件数、対策件数、ボタン上の件数（参考情報の件数は残す）、「未同期」表示、フッターの「最終同期」
- データから消したもの: `synced`、`combo`、`matchup`（旧データにあれば移行時に捨てる）
- 残るもの: 習得数・★（localStorage）、更新日（参考情報の更新日と登録日）、最近の更新

**結果**: ポータルは「導線 + 参考情報 + 習得状況の表示」に絞られ、要件は実装完了。残るのはキャラ対策ツールのみ。

---

## 18. 背景画像（3画面共通の提案、2026-09-20）

**要望**: ポータル・コンボDB・VSキャラ対策DB の Web 背景に、グレースケール化した画像を入れたい。

### 18.1 仕組み

- 画像ファイルはそのまま使い、**CSS の `filter: grayscale(1)` でグレースケール化**する。変換済み画像を別に用意する必要はない（「色あり」への切替も CSS だけでできる）
- `position: fixed` の背景レイヤーをページの下に敷き、`opacity` で濃さを調整。`mix-blend-mode: multiply` で重ねると画像の明るい部分は紙色のまま残り、暗い部分だけが沈むので、ライト・ポップの紙色が保たれる
- カードは白なので、本文の可読性は背景の濃さに影響されない。影響を受けるのは見出し・チップまわりの余白部分だけ
- 画像は `画像/<キャラ名>/` の相対パスで参照（file:// で動く）。キャラ画像（カード内の乗算表示）とは別に「背景用」の画像を選べるようにする

### 18.2 敷き方5案（モック: `デザイン案_背景.html`。上部パネルで敷き方・画像・濃さ・色あり・画面を切替。URL に `#B3,vs,30` のように指定すると初期状態を変えられる）

| 案 | 敷き方 | 向き |
|---|---|---|
| **B1 全面うっすら** | 画面全体に固定、グレースケール 10〜20% | 最も単純。どの画面にも同じ扱いで入れられる |
| **B2 上部だけ** | 上部 560px に置き、下へ向かって紙色に溶かす | 見出し付近だけ雰囲気を出し、一覧部分は紙のまま。動画が並ぶコンボDB / VS DB に向く |
| **B3 右にキャラ** | 右寄せ、左から右へ現れるマスク | キャラの顔が右に出て、左の見出し・チップと干渉しない。キーアートの構図（右に文字）と相性が良い |
| **B4 ハーフトーン** | B3 + 印刷風のドット | 紙感が強まる。濃くすると騒がしいので 20% 前後まで |
| **B5 帯の中だけ** | ページ本体には敷かず、種別バナーの中だけに乗算で薄く | 最も控えめ。ポータルにはバナーが無いので、ポータルだけ別案（B1 か B3）が必要 |

**推奨: コンボDB / VS DB は B3（20〜30%）、ポータルは B1（10〜15%）。** B3 はキャラの顔が右に出て画面ごとの個性が出る一方、動画カードの上には画像がほとんど来ない。ポータルは全キャラが並ぶので特定キャラの顔を大きく出すより、B1 で薄く敷くほうが落ち着く。

### 18.3 実装するときのデータ

- コンボDB / VS DB: `bg: { "image": "画像/RASHID/topic-08152-1.jpg", "mode": "B3", "opacity": 0.25 }` をデータに追加。編集バーに「背景画像を選ぶ」「濃さ」を置く（キャラ画像の選択と同じ仕組み）
- ポータル: `bg` を1つ持つ（どのキャラの画像でもよい）。将来「自キャラの先頭の背景を使う」も可能
- 未設定なら背景なし（今のまま）

### 18.4 決定（2026-09-20）

- 敷き方: **B1 全面うっすら**（3画面共通）
- 既定の濃さ: **色あり 45%**
- グレースケール / 色あり を **選べるようにする**

### 18.5 スクロール時の画像切替（追加要望）

**質問**: 縦にスクロールして画像が途切れたらどうなるか。次の別の画像を参照してほしい。
**前提**: B1 は画像を画面に固定（`position: fixed`、`background-size: cover`）しているので、スクロールしても**途切れない**。ただし1枚のまま。複数の画像を順に見せる方式を5案（モック: `デザイン案_背景2.html`。上部パネルで方式・濃さ・グレースケールを切替、URL `#S2,1500` で方式と初期スクロール位置を指定）。

| 案 | 動き | 途切れ | 特徴 |
|---|---|---|---|
| **S1 固定・スクロールでフェード** | 画像は固定のまま、スクロール量が一定（1400px）進むごとに次の画像へクロスフェード | なし | B1 の性質（常に画面いっぱい）を保ったまま画像が変わる。最後の画像の後は1枚目に戻る |
| **S2 縦に並べて流す** | 画像を1画面分ずつ縦に並べ、本文と一緒にスクロール。ページの長さに合わせて繰り返す | なし（繰り返しで埋める） | 紙をめくる感じ。画像の境目が見える |
| **S3 パララックス** | S2 と同じ縦並びで、背景だけ本文の半分の速さで動く | なし | 奥行きが出る。境目の通過がゆっくり |
| **S4 帯で区切る** | 画像を高さ 70vh の帯として一定間隔で置き、上下を紙色に溶かす。本文と一緒にスクロール | 帯と帯の間は紙色 | 「途切れ」を意図的に見せる案。各帯が別の画像 |
| **S5 固定・時間でフェード** | 画像は固定、スクロールと無関係に 8 秒ごとにクロスフェード | なし | 読んでいる最中に背景がゆっくり変わる。短いページでも全画像が見える |

**推奨: S1。** B1 の決定（全面・固定）を変えずに「次の画像へ」の要望を満たす。ページが短くて切替点に届かない場合の保険として S5（時間で切替）を併用してもよい。S2/S3 は本文と一緒に動くぶん賑やかで、動画カードの下を背景が流れる。

**決定（2026-09-20）**: 切替はせず **B1 のまま**（`position: fixed` で画面に固定、1枚、スクロールしても動かず途切れない）。S1〜S5 は採用しない。

### 18.6 実装（2026-09-20）

3ツールに同じ仕組みを入れた。

| 項目 | 内容 |
|---|---|
| 表示 | `<div class="bgimg">` を `position: fixed; inset: 0` で最背面に置き、`background-size: cover`、`mix-blend-mode: multiply`。本文（バナー・`.wrap`）は `z-index: 1` で上に載せる |
| データ | `bg: { "image": "画像/RASHID/topic-08152-1.jpg", "opacity": 0.45, "gray": false }`。パスは **`スト６/` 基準**で保存し、サブフォルダにあるコンボDB / VS DB は表示時に `../../` を補う |
| 編集バー | 「背景画像: 画像を選ぶ / 外す / パス表示 / 濃さ（5〜80%）/ グレースケール」。既定は色あり 45% |
| 画像の選び方 | コンボDB は `画像/<キャラ名>/`、VS DB は `画像/<相手キャラ名>/` のファイルを選ぶ（ファイル名だけ記録）。**ポータルは 2026-09-20 に変更**: 「画像を選ぶ」で初回だけ `スト６/画像` フォルダを接続し、続けてファイルを選ぶと、画像フォルダからの相対パス（サブフォルダはどれでも）を File System Access API の `resolve()` で求めて記録する。パス欄に `画像/…` を直接入力してもよい（`画像/` で始まらない値は拒否、`\` と先頭の `./` は正規化） |
| 保存 | HTML保存で `bg` を書き出し。実行時のプレビュー（blob）や inline style は保存前に除去。JSON書き出し／読み込みも `bg` を含む |
| 既定値 | ポータル: `画像/RASHID/y_64a3cd87d8348.jpg`。ラシードのコンボDB: `画像/RASHID/topic-08152-1.jpg`。テンプレートは背景なし |

確認（headless Chrome）: 3ツールとも JS エラーなし。ラシードのコンボDB で、濃さ・グレースケールの変更がデータと表示に反映されること、保存用 HTML に `bg` が入り inline style が消えること、「外す」で非表示になることを確認。

---

## 19. カードデザインの提案（3画面共通、2026-09-20）

**要望**: 白背景が多く寂しい。ポータル・コンボDB・VS DB のカードに複数案がほしい。
**前提**: 背景画像（B1、色あり 45%）の上にカードが載る。カードの骨格（3px 黒線、硬い影、角丸）は保つ。

### 19.1 5案（モック: `デザイン案_カード.html`。上部タブで切替。ポータルの大カードと小タイル、コンボDB、VS DB のカードを同じ画面で比較）

| 案 | 変えるところ | 白の減り方 | 注意点 |
|---|---|---|---|
| **K1 カラーヘッダ** | カードの見出し部（タグ + レシピ / タイトル、ポータルは名前）をキャラ色の帯に、本文は白のまま。ポータルは帯とボタン列の色が入れ替わる | 半分ほど | 文字は白 + 黒影で可読性を確保。キャラ色が淡い場合は帯の上の白文字が弱くなる |
| **K2 ティント** | カード全体をキャラ色 12% で淡く染め、本文の左に太いキャラ色の帯。ポータルの大カードはグラデーション | 全面（淡い） | 最も穏やか。白ではないが「色が付いた紙」の印象 |
| **K3 ダーク反転** | カードを暗色にし、動画の黒と一体化。影をキャラ色に。文字は白 | 全面 | 明るい背景に暗いカードが浮く。**ポータルのキャラ画像（白背景）は暗いカードでは白い箱が見えてしまう**ので、透過 PNG が必要 |
| **K4 パターン** | 白カードのまま、右上に斜めストライプの角、右下に透かし文字（COMBO / VS）、見出しと本文の間はキャラ色の点線 | 変わらない（装飾で埋める） | 白は残るが賑やかになる。装飾が多いと本文と競合する |
| **K5 ガラス** | カードを半透明（白 72% + ぼかし）にして背景画像を透かす | 全面（背景が透ける） | 背景画像を入れた意味が最も出る。可読性はぼかしで確保。ぼかしは描画負荷がやや高い |

**推奨: K1 か K5。** 白を減らしつつ骨格を保てるのは K1（キャラ色を主役にする）と K5（背景を主役にする）。両立させて「K5 の半透明 + K1 の見出し帯」も可能。K2 は変化が小さく、K3 は画像の問題があり、K4 は装飾で誤魔化す形になる。

### 19.2 決定（2026-09-20）

- コンボDB / VS DB: **K5 ガラス**（半透明 + ぼかし）を採用 → **2026-09-20 実装済み**。カードは白 72% + `backdrop-filter: blur(8px)`、動画の操作バーは白 72%（ぼかしなし。入れ子の backdrop-filter は Chrome で描画されないことがある）、編集バー・フォーム・方針欄は白 82% + ぼかし。テンプレートとラシードに適用
- ポータル: 別途、SF6 のタイトルロゴ風の幾何学デザインにする（20章）

---

## 20. ポータルのカード: ロゴ風の幾何学デザイン（2026-09-20）

**要望**: キャラ色は主張しすぎない程度に取り入れつつ、SF6 のタイトルロゴ（`画像/StreetFighter6/logo_mark.png`）のような幾何学的なデザインにしたい。
**ロゴの要素**: 六角形の立方体（「6」の形）、中央の斜めの切れ込み、炭色 + グラフィティ質感、オレンジの飛沫。これを部品に分解し、キャラ色は縁・一面・細い帯・小さなバッジに限定する。

### 20.1 4案（モック: `デザイン案_ポータルカード.html`、上部タブで切替。背景画像の上に自キャラ2枚と小タイル6枚）

| 案 | 幾何学の要素 | キャラ色の使いどころ | 印象 |
|---|---|---|---|
| **G1 ヘックスエンブレム** | 角を落とした八角形のカード + 六角形の紋章（炭色、頭文字入り） | 紋章の縁（3px）と名前の下の細い線の 34% だけ | 白カードで軽い。紋章が識別子になる。キャラ色は最小 |
| **G2 アイソキューブ** | ロゴの立方体を等角投影で右上に。上面グレー、左面炭色 | 右面だけ炭色にキャラ色を 45% 混ぜたもの + 稜線 | 最もロゴに近い形。カードは白、ボタン帯に斜めストライプ |
| **G3 スラッシュカット** | 炭色のヘッド部の下辺を斜めに切る（ロゴ中央の切れ込み）。カード右下も斜めに落とす | 切れ込みに沿った細い帯（8px） | 名前が白抜きで力強い。小タイルは切れ込みを下げて名前を確保 |
| **G4 ハニカム** | カード全体を炭色にし、六角格子（7%）を敷く。右上に六角バッジ、ロゴの飛沫を模したドット | 六角バッジ + 飛沫のドット（55%） | ロゴの雰囲気（暗色 + 質感 + 飛沫）に最も近い。明るい背景に暗いカードが浮く |

**推奨: G4 か G1。** 「ロゴらしさ」なら G4（炭色 + 六角格子 + 飛沫）、ライト・ポップの白い骨格を保ちたいなら G1（八角形 + 六角紋章）。G2 は立方体が名前と場所を取り合い、長い名前で窮屈。G3 は小タイルで切れ込みの余地が少ない。

### 20.2 決定（2026-09-20）

**G1 ヘックスエンブレム** を採用。さらに、六角形の紋章にはキャラごとの自作イメージロゴ画像を入れる。既定は `画像/StreetFighter6/20240408020940.png`（SF6 ロゴ、透過 PNG 1070×913）。各キャラの紋章画像を作るプログラムは後で作る。

### 20.3 実装（`index.html`）

| 項目 | 内容 |
|---|---|
| カード | 角を落とした八角形（`clip-path`、外側 3px が黒線、影は `drop-shadow`）。白地。小タイル・大カードとも同じ構造 |
| 紋章 | 左に六角形（`clip-path`）。縁 3px は黒（2026-09-20: キャラ色から変更）、中は炭色 `#1f2026` の上に紋章画像を `object-fit: cover` で表示 |
| 紋章画像の決め方 | 1. キャラの `emblem`（フォームで選んだパス）→ 2. 無ければ **`画像/<フォルダ名>/emblem.png`** を試す → 3. 読めなければ既定画像 `画像/StreetFighter6/LTyBkxp-_400x400.jpg`（2026-09-20 変更）→ 4. それも無ければ頭文字。**生成プログラムは `画像/<キャラ名>/emblem.png` を置くだけでよく、データを触る必要はない** |
| 名前 | 紋章の右に Bebas Neue。小タイルは 5文字以上 20px、6文字以上 17px、7文字以上 15px |
| 名前の下の線 | 4px。左 34% がキャラ色、残りが炭色 |
| ボタン帯 | 炭色。白い縁のボタン（ホバー・展開中は白地に黒文字）。大カードは「COMBO DB / VS DB / Information」、**小タイルは「COMBO / VS / INFO」**（件数表示は 2026-09-20 に削除）（幅が狭く文字がつぶれるため 2026-09-20 に省略形へ）。Bebas Neue、中央揃え |
| キャラ画像 | 大カードのみ、従来どおり右上に乗算で薄く表示（32%、ホバーで 80%）。小タイルには出さない |
| データ | キャラに `emblem`（任意、`スト６/` 基準のパス）を追加。編集フォームに「紋章を選ぶ / 既定に戻す」。選んだ直後はプレビュー、保存でパスを記録 |
| 削除したもの | 画像なしキャラのドット柄、小タイルの頭文字の透かし（紋章が識別子になるため） |

確認（headless Chrome）: カードの構造（八角形・紋章）、`emblem.png` が無いときの既定ロゴへのフォールバック、キャラ追加、参考情報の展開、紋章の「既定に戻す」、保存用 HTML の生成。8キャラ（長い名前・A.K.I. を含む）でレイアウト崩れなし。

---

## 21. ポータルのカード: 全面画像 + 右下に名前（2026-09-20）

**要望**: 今は白背景 + 画像1枚だが、カード全体を画像1枚の背景にしたい。紋章は不要。キャラ名は白文字・幾何学的なフォントで右下に。

### 21.1 共通の考え方

- カードの表面（ボタン帯の上）にキャラの画像を `cover` で敷く。画像はキャラの `image`（今の乗算表示に使っているもの）を流用するが、**全面に敷くならキーアートのような横長の絵が向く**（白背景の小さい立ち絵は粗く見える）
- 画像が無いキャラはキャラ色の面 + 斜線 + 頭文字の透かしで代替
- 名前は右下に白。可読性はグラデ、斜め面、デュオトーンのいずれかで確保
- 紋章（六角形）と名前の下の線は廃止。統計（習得・★・更新）は左上か右上の小さな白文字

### 21.2 5案（モック: `デザイン案_ポータルカード2.html`。上部タブで切替。ケンはケンの画像が無いためラシードの別画像で代用）

| 案 | 名前の読ませ方 | ボタン帯 | フォント | 特徴 |
|---|---|---|---|---|
| **F1 下グラデ** | 下へ向かって暗くなるグラデの上に右下 | 炭色（画像の下） | Bebas Neue | 最も素直。画像の下 40% が暗くなる |
| **F2 全面 + 半透明ボタン** | 名前は右上、統計は左上。画像の下半分に黒 45% の半透明ボタンを重ねる | 画像の上に重ねる | Bebas Neue | 画像が最も大きく見える。ボタンの下の画像は暗くなる |
| **F3 斜めの名前帯** | 右下に炭色の斜め面（SF6 ロゴの切れ込み）を置き、その中に名前。斜め面の左端にキャラ色の細い帯 | 炭色 | Bebas Neue | 可読性が画像に依存しない。幾何学的な形が残る |
| **F4 デュオトーン** | 画像をグレースケールにしてキャラ色で染める。名前は右下 | 白 | Russo One | カードごとの色が揃い、一覧が整う。画像本来の色は消える |
| **F5 角落とし維持** | G1 の八角形と黒線・硬い影を保ち、中身を全面画像に。名前は右下 | 炭色 | Teko | ロゴの多角形らしさと今の骨格を残す |

**推奨: F3 か F5。** 名前の可読性を画像に依存させず、SF6 ロゴの幾何学（斜めの切れ込み / 多角形）が残る。F1 は無難、F2 は画像重視、F4 はキャラ色を全面に出したい場合。

**フォント**: Bebas Neue（現行、細身のコンデンス）/ Russo One（四角い幾何学ゴシック、ロゴに近い）/ Teko（コンデンスで太め）。名前が日本語のときは各フォントに日本語が無いので Noto Sans JP 900 に落ちる。**日本語の名前は結局 Noto Sans JP で描かれる**ため、フォントの違いは英字（RASHID など）にしか出ない。日本語名を幾何学的にしたいなら、英語表記（RASHID）を主にして日本語を小さく添える案を検討する

### 21.3 決定と実装（2026-09-20）

**決定**: F5（角落とし維持 + 全面画像 + Teko）を採用。追加要望として「その他のキャラは、カーソルが当たったときだけカラー、それ以外はグレースケール」のモード。

| 項目 | 実装 |
|---|---|
| カード | G1 の八角形（`clip-path`、黒線 3px、`drop-shadow`）を維持し、中身を全面画像に。画像はキャラの `image` を `cover` で敷き、下 65% から暗くなるグラデを重ねる |
| 名前 | 右下に白、Teko 700（日本語は Noto Sans JP 900 に落ちる）。大カード 64px、小タイル 34px（5文字以上 26px、6文字以上 22px、7文字以上 19px） |
| 統計 | 左上に白い小文字（習得は緑、★は黄） |
| 画像なし | キャラ色の面 + 斜線 + 左上に頭文字の透かし |
| 紋章 | 撤去（フォームの項目、データの `emblem`、既定画像のフォールバックをすべて削除。旧データの `emblem` は読み込み時に捨てる） |
| ボタン帯 | 炭色、「COMBO DB / VS DB / Information n」 |
| **グレースケールモード** | 編集バー「表示」の「その他のキャラをグレースケールにし、カーソルを当てたときだけカラーにする」。ON のとき ROSTER のカード画像に `grayscale(1)`、ホバーで `filter: none`（0.25 秒でフェード）。自キャラ枠は常にカラー。設定はデータの `view.grayRoster` に保存。**既定は ON** |
| 画像の検証 | 背景画像として敷くため、`Image` で読み込みを試し、失敗したカードは画像なし表示に落として編集モードで警告を出す |
| 既定データ | ラシードの `image` をキーアート `画像/RASHID/topic-08152-1.jpg` に変更（全面に敷くのに向くため。以前の小さな立ち絵は背景画像用途に不向き） |

未決のまま実装した点: 名前は日本語のまま（英語表記を主にする案は採らず）。画像は今の `image` を全面用にも使う（全面用の別画像は持たない）。変えたければ指示を。

確認（headless Chrome）: JS エラーなし、グレースケールモードの ON/OFF がデータと本文クラスに反映、8キャラのプレビューで画像あり・なしの両方が崩れないこと、参考情報の展開、保存用 HTML の生成（`view` を含む）。

---

## 22. キャラ名（英語）のフォント（2026-09-20）

**指摘**: ポータルのキャラ名を英語にすると、Teko 700 の字面が太くて気になる。

### 22.1 8案（モック: `デザイン案_名前フォント.html`。上部タブで切替。名前を英語にした F5 カードで比較）

| 案 | 書体 | 印象 | 備考 |
|---|---|---|---|
| T0 現行 | Teko 700 | 太く重い | 比較用 |
| **T1** | Teko 400 | 骨格そのままで線だけ細い | 変化が最小。現行の雰囲気を保ちたいなら |
| **T2** | Bebas Neue | 見出し（SF6 PORTAL、COMBO DB）と同じ書体 | 統一感が最も高い。字幅は Teko より広い |
| T3 | Oswald 300 | 細身で落ち着く | 幾何学というより新聞見出し寄り |
| T4 | Rajdhani 500 | 角を落とした幾何学体、メカ寄り | SF6 の UI に近い。字幅が広く長い名前は小さくなる |
| T5 | Saira Condensed 400 | 幾何学 + コンデンスの中間 | 癖が少ない |
| T6 | Teko 700 の中抜き | 太さは同じでも塗りが無く軽い | 小タイルでは線が細くなり読みにくい |
| T7 | Barlow Condensed 400 + キャラ色の下線 | 細字にキャラ色を添える | キャラ色を名前の近くに置ける |

**推奨: T2（Bebas Neue）か T1（Teko 400）。** T2 はページ全体の書体が揃い、T1 は現行の形を保てる。幾何学的な方向を強めたいなら T4。

日本語名は各書体に無いため引き続き Noto Sans JP 900 で描かれる。日本語名を細くしたい場合は Noto Sans JP 700 に落とすことも可能。

### 22.2 決定と実装（2026-09-20）

**T2（Bebas Neue）を採用。** `index.html` のキャラ名を Bebas Neue に変更し、Teko の読み込みを外した。サイズは大カード 60px、小タイル 32px（5文字以上 25px、6文字以上 21px、7文字以上 18px）、字間 .05em。
日本語名は Noto Sans JP 700 のまま。Bebas Neue は 400 しか無いため、`font-weight: 700` を指定するとブラウザが擬似的に太らせてしまう。`font-synthesis: none` でそれを止め、英字は Bebas 400 のまま、日本語だけ 700 で描く。
21.3 の「名前」の記述（Teko 700）はこの節の内容で上書き。
名前を縮める判定は文字数ではなく見た目の長さで行う（全角 1、英数字 0.55）。Bebas の英字は細いので、`CHUN-LI` や `ZANGIEF` は縮めずに済む。
